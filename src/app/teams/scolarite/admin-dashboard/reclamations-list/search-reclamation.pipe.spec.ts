import { SearchReclamationPipe } from './search-reclamation.pipe';

describe('SearchReclamationPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchReclamationPipe();
    expect(pipe).toBeTruthy();
  });
});
